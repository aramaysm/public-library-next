
import { styled } from "@mui/material/styles";
import {Chip} from "@mui/material"


export const Chip_Success = styled(Chip)({
    backgroundColor: "#ceffca",
    color: "#0c9200",
    
});

export const Chip_Error = styled(Chip)({
    backgroundColor: "#ffded9",
    color: "#f44336",
    
});